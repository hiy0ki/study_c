#include <stdio.h>

int main(void)
{
    int len,width;

    printf("pls input len : ");
    scanf("%d", &len);

    printf("pls input width : ");
    scanf("%d", &width);

    printf("result : %d \n", len * width);

    return 0;
}
